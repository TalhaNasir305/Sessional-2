import styled from 'styled-components'
import {Links} from 'react-scroll'

export const Button=Styles{Link}`
border-radius:50px;
background: &{{{primary}} =>{primary ? '#02BF72' : '#020606'}};
white-space:nowrap;
paddind :&{{{big}} => {big ? '16px':'12px 30px'}}
color: &{{{dark}} => (dark ? '01606' : '#fff' )}

outline:none;
cursor:pointer;
display:flex;
justify-content:center;
transition: all 0.2s ease-in-out;
`